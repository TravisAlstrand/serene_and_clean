import { MenuOutlined } from "@ant-design/icons";

const Header = () => {
  return (
    <header>
      <div className="lg:hidden">
        <MenuOutlined />
      </div>
    </header>
  );
};

export default Header;
